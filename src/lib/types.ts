export type ProductType = {
  id: number;
  title: string;
  description: string;
  price: number;
  imagePath: string;
  sellerKindeId: string;
}

export type UserType = {
    id: number;
    name: string;
    email: string;
}