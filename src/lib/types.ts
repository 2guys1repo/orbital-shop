export type ProductType = {
  id: number;
  title: string;
  description: string;
  price: number;
  imagePath: string;
  sellerId: number;
}

export type UserType = {
    id: number;
    name: string;
    email: string;
}